function solution(str, ending){
    // TODO: complete
  
    let splitStr = str.split('')
    let splitEnding = ending.split('')
    console.log(splitStr, splitEnding);
    let strEnding = "";
    console.log(splitStr.length);
    

    // for(let i = splitStr.length - 1; i > ((splitEnding.length - 1) - splitEnding.length); i--) {
    //     strEnding = splitStr[i];
    //     strEnding = strEnding + strEnding;
    // }
    // console.log(strEnding)
  }